function convert(){
    const text=Number(document.getElementById("input").value)
    const covert=text+273+"f"
    const result=document.getElementById("result")
     result.innerHTML=covert
     
}
